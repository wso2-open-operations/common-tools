# QR Code Generation Service - Environment Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# Server Configuration
# ============================================================================

# HTTP server port
# Default: 8080
PORT=8080

# ============================================================================
# Timeout Configuration
# ============================================================================

# Maximum duration for reading the entire HTTP request (including body)
# Format: Valid Go duration string (e.g., "5s", "100ms", "1m30s")
# Default: 5s
READ_TIMEOUT=5s

# Maximum duration before timing out writes of the HTTP response
# Format: Valid Go duration string
# Default: 10s
WRITE_TIMEOUT=10s

# Timeout for graceful shutdown when receiving SIGINT or SIGTERM
# Format: Valid Go duration string
# Default: 5s
SHUTDOWN_TIMEOUT=5s

# ============================================================================
# Security Configuration
# ============================================================================

# Maximum allowed request body size in bytes
# This prevents DoS attacks via large payloads
# Default: 524288 (512KB)
# Example values:
#   - 512KB: 524288
#   - 1MB: 1048576
#   - 2MB: 2097152
#   - 5MB: 5242880
MAX_BODY_SIZE=524288

# ============================================================================
# QR Code Configuration
# ============================================================================

# Minimum QR code size in pixels
# Default: 64
# Note: Sizes below 64px are not practical for scanning
MIN_SIZE=64

# Maximum QR code size in pixels
# Default: 2048
# Note: Larger sizes increase processing time and memory usage
MAX_SIZE=2048

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level determines which messages are logged
# Levels: debug, info, warn, error
# - debug: Most verbose, includes all messages (use for development/debugging)
# - info: Standard informational messages (recommended for production)
# - warn: Warning messages and errors only
# - error: Error messages only
# Default: info
LOG_LEVEL=info

# Log environment determines the output format
# - prod: JSON format for structured log parsing (recommended for production)
# - dev: Human-readable text format (recommended for development)
# Default: dev
LOG_ENV=dev

# ============================================================================
# Usage Examples
# ============================================================================

# Development (verbose logging, human-readable):
# LOG_LEVEL=debug
# LOG_ENV=dev

# Production (structured logging, essential messages only):
# LOG_LEVEL=info
# LOG_ENV=prod

# Production with debug logging (for troubleshooting in Choreo/K8s):
# LOG_LEVEL=debug
# LOG_ENV=prod

# ============================================================================
# Notes for Choreo Deployment
# ============================================================================

# When deploying to Choreo:
# 1. Set LOG_ENV=prod for JSON formatted logs
# 2. Set LOG_LEVEL=debug temporarily when debugging issues
# 3. Set LOG_LEVEL=info for normal operation
# 4. Adjust timeouts based on your use case
# 5. Set MAX_BODY_SIZE based on expected payload sizes
