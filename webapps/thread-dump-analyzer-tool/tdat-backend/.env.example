# Anthropic API key used by internal/ai/insights.go to generate the executive
# summary, pattern recognition, and recommended actions. If unset, the analysis
# job still completes and ai_insights returns a static "unavailable" message.
ANTHROPIC_API_KEY=

# Optional. Set to "true" to enable rs/cors debug logging on every request.
CORS_DEBUG=false
